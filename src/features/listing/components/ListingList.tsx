import type { Listing } from "../types";

interface Props {
    listings: Listing[]
}

const ListingList: React.FC<Props> = () => {

    return (
        <>
        
        </>
    )
}

export default ListingList